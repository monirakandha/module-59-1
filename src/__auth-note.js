/**
 * create a new project in googlefirebase 
 * npm install
 * create firbase.int.js and import getauth and export getauth
 * firebase setting > authentication > enable email and password auth
 * create login signup components setup router
 * attach form field handler and form submit handeler
 * npm install --save react-firebase-hooks
 * use createuserwith emailpassword form react firebase hook
 *  useSignInWithEmailAndPassword use for singin
 * 
 */

/**
 * hosting steps
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 3. firebase init
 * 4. firebase deploy
 */